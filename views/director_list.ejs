<%- include header %>

    <div class="content">
        <h1>List of Directors</h1>

        <div class="movie_list">
            <ul>
                <% if (director_list.length > 0) { %>
                    <% for(var i = 0;i < director_list.length; i++) { %>
                            <li style="margin-bottom: 10px"><a href=<%=director_list[i].url%> ><%= director_list[i].name %></a> </li>
                    <% } %>
                <% } %>

            </ul>
        </div>
    </div>
    
    <body>
<html>


